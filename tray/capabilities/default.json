{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "The main window may use the core APIs and the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
