const E = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const shapes = {
 overview:'<rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/>',
 ports:'<path d="M8 3v5m8-5v5M6 8h12v4a6 6 0 0 1-12 0V8zm6 10v3"/>',
 history:'<path d="M3 11a9 9 0 1 1 2 7M3 4v7h7m2-5v6l4 2"/>',
 settings:'<path d="M4 7h16M4 17h16"/><circle cx="9" cy="7" r="3" fill="var(--side)"/><circle cx="15" cy="17" r="3" fill="var(--side)"/>',
 search:'<circle cx="10" cy="10" r="6"/><path d="m15 15 6 6"/>',
 lock:'<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/>',
 folder:'<path d="M3 7V5h6l3 3h9v12H3V7z"/>',
 chevron:'<path d="m7 10 5 5 5-5"/>',
 globe:'<circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a18 18 0 0 1 0 18 18 18 0 0 1 0-18"/>',
 check:'<path d="m5 12 4 4L19 6"/>',
 info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v6m0-10v1"/>',
 theme:'<path d="M20 15A9 9 0 0 1 9 4a9 9 0 1 0 11 11z"/>'
};
function icon(name, cls=''){return `<svg class="icon ${cls}" viewBox="0 0 24 24" aria-hidden="true" focusable="false">${shapes[name]||shapes.info}</svg>`;}
const memory = n => n>=1024 ? (n/1024).toFixed(1)+' GB' : Math.round(n)+' MB';
const elapsed = n => n>=86400 ? Math.floor(n/86400)+'d '+Math.floor(n%86400/3600)+'h' : n>=3600 ? Math.floor(n/3600)+'h '+Math.floor(n%3600/60)+'m' : n>=60 ? Math.floor(n/60)+'m' : 'Just now';
const compactElapsed = n => n>=86400 ? Math.floor(n/86400)+'d' : n>=3600 ? Math.floor(n/3600)+'h' : n>=60 ? Math.floor(n/60)+'m' : '<1m';
const datasets = {
 sessions:[
 {id:'s1',name:'Refactor the billing settings',context:'~/code/atlas',group:'atlas',memory:1229,idle:64800,state:'stale',host:'Terminal',age:'2 days',cpu:'0.0%',pid:4101},
 {id:'s2',name:'Build the usage report',context:'~/code/atlas',group:'atlas',memory:1126,idle:0,state:'active',host:'VS Code',age:'2 hours',cpu:'12.4%',pid:4104},
 {id:'s3',name:'Add pagination to search results',context:'~/code/ledger',group:'ledger',memory:922,idle:2640,state:'idle',host:'Terminal',age:'3 hours',cpu:'0.1%',pid:4105},
 {id:'s4',name:'Fix the onboarding redirect',context:'~/code/atlas',group:'atlas',memory:819,idle:39600,state:'stale',host:'Terminal',age:'1 day',cpu:'0.0%',pid:4102},
 {id:'s5',name:'Review the database migration',context:'~/code/ledger',group:'ledger',memory:614,idle:28800,state:'stale',host:'Terminal',age:'1 day',cpu:'0.0%',pid:4103},
 {id:'s6',name:'Investigate duplicate webhook deliveries after reconnecting',context:'~/code/atlas/integrations',group:'atlas',memory:410,idle:9900,state:'idle',host:'Terminal',age:'5 hours',cpu:'0.1%',pid:4106},
 {id:'s7',name:'Desktop app engine',context:'~/code/autoTrim',group:'autoTrim',memory:256,idle:0,state:'active',protected:'App engine',host:'Claude desktop',age:'4 hours',cpu:'1.2%',pid:4107},
 {id:'s8',name:'Tighten the memory estimates',context:'~/code/autoTrim',group:'autoTrim',memory:184,idle:4500,state:'idle',host:'Terminal',age:'2 hours',cpu:'0.0%',pid:4108}
 ],
 tabs:[
 {id:'t1',name:'Planning the account settings',context:'claude.ai',group:'claude.ai',profile:'Work',memory:358,idle:259200,state:'stale',url:'https://claude.ai/chat/demo-account-settings'},
 {id:'t2',name:'Debugging the migration',context:'chatgpt.com',group:'chatgpt.com',profile:'Work',memory:358,idle:180000,state:'stale',url:'https://chatgpt.com/c/demo-migration'},
 {id:'t3',name:'Project board',context:'github.com',group:'github.com',profile:'Work',memory:358,idle:172800,state:'stale',protected:'Pinned',url:'https://github.com/example/atlas/projects/1'},
 {id:'t4',name:'API design notes',context:'github.com',group:'github.com',profile:'Work',memory:358,idle:115200,state:'stale',url:'https://github.com/example/atlas/discussions/42'},
 {id:'t5',name:'Weekend reading',context:'developer.mozilla.org',group:'developer.mozilla.org',profile:'Personal',memory:358,idle:93600,state:'stale',url:'https://developer.mozilla.org/en-US/docs/Web/CSS/grid'},
 {id:'t6',name:'Notes for the next iteration',context:'claude.ai',group:'claude.ai',profile:'Work',memory:358,idle:18000,state:'idle',url:'https://claude.ai/chat/demo-next-iteration'},
 {id:'t7',name:'Release checklist',context:'github.com',group:'github.com',profile:'Work',memory:358,idle:10800,state:'idle',url:'https://github.com/example/atlas/issues/128'},
 {id:'t8',name:'Train times',context:'amtrak.com',group:'amtrak.com',profile:'Personal',memory:358,idle:7200,state:'idle',url:'https://www.amtrak.com/'},
 {id:'t9',name:'A complete guide to CSS grid layout, track sizing, and alignment',context:'developer.mozilla.org',group:'developer.mozilla.org',profile:'Work',memory:358,idle:3600,state:'idle',url:'https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout'},
 {id:'t10',name:'Inbox',context:'mail.google.com',group:'mail.google.com',profile:'Work',memory:358,idle:1500,state:'idle',protected:'Pinned',url:'https://mail.google.com/mail/u/0/#inbox'},
 {id:'t11',name:'Atlas dashboard',context:'localhost:5173',group:'localhost:5173',profile:'Work',memory:358,idle:900,state:'idle',url:'http://localhost:5173/dashboard'},
 {id:'t12',name:'autoTrim pull requests',context:'github.com',group:'github.com',profile:'Work',memory:358,idle:0,state:'active',url:'https://github.com/example/autoTrim/pulls'}
 ],
 ports:[
 {id:'p1',name:'Atlas development server',context:'node · PID 5221',group:'atlas',port:5173,address:'127.0.0.1',protocol:'TCP',memory:410,idle:93600,state:'stale',host:'Terminal',age:'1 day 2 hours'},
 {id:'p2',name:'Ledger API',context:'python · PID 6840',group:'ledger',port:8000,address:'127.0.0.1',protocol:'TCP',memory:184,idle:7200,state:'idle',host:'Terminal',age:'2 hours'},
 {id:'p3',name:'Storybook component preview',context:'node · PID 5288',group:'atlas',port:6006,address:'127.0.0.1',protocol:'TCP',memory:220,idle:14400,state:'idle',host:'Terminal',age:'4 hours'},
 {id:'p4',name:'Local PostgreSQL',context:'postgres · PID 719',group:'System & apps',port:5432,address:'127.0.0.1',protocol:'TCP',memory:132,idle:0,state:'active',protected:'Managed',host:'Postgres.app',age:'5 days'},
 {id:'p5',name:'Docker port forwarding',context:'com.docker.backend · PID 998',group:'System & apps',port:8080,address:'0.0.0.0',protocol:'TCP',memory:97,idle:0,state:'active',protected:'Managed',host:'Docker',age:'1 day'},
 {id:'p6',name:'AirPlay Receiver',context:'ControlCenter · PID 412',group:'System & apps',port:7000,address:'*',protocol:'TCP',memory:83,idle:0,state:'active',protected:'System',host:'macOS',age:'5 days'}
 ],
 trends:[
 {id:'r1',name:'Google Chrome',context:'12 browser tabs',group:'Browsers',memory:8602,change:840,rate:420,cpu:'1.2%',span:'2 hours'},
 {id:'r2',name:'Claude Code',context:'8 agent sessions',group:'Agent sessions',memory:6349,change:-120,rate:-60,cpu:'12.5%',span:'2 hours'},
 {id:'r3',name:'Codex',context:'2 agent sessions',group:'Agent sessions',memory:3174,change:184,rate:92,cpu:'3.1%',span:'2 hours'},
 {id:'r4',name:'Cursor',context:'24 processes',group:'Apps',memory:2048,change:96,rate:48,cpu:'0.7%',span:'2 hours'},
 {id:'r5',name:'Docker',context:'8 processes',group:'Apps',memory:1331,change:-64,rate:-32,cpu:'0.1%',span:'2 hours'},
 {id:'r6',name:'Slack',context:'16 processes',group:'Apps',memory:819,change:34,rate:17,cpu:'0.3%',span:'2 hours'}
 ]
};
const directions = [
 {id:'quiet',file:'01-quiet-rows.html',title:'Quiet rows',description:'A simple table with fine dividers, open space, and restrained actions. The easiest change to live with.',tradeoff:'Best for scanning across columns. More vertical space than a dense list.'},
 {id:'grouped',file:'02-grouped-rows.html',title:'Grouped rows',description:'Projects and sites become the organizing structure. Select what to close once, instead of repeating a button on every row.',tradeoff:'Best for cleaning up one project or site. Global memory order is less direct.'},
 {id:'split',file:'03-split-view.html',title:'Compact list + details',description:'Keep the list short and consistent. Select a name to read its full details in a dedicated pane.',tradeoff:'Best for long lists and inspecting one item. Details need a click; the pane stacks in narrow windows.'}
];
const config = {
 sessions:{title:'Claude Code',type:'Agent',subject:'session',plural:'sessions',sub:'6.2 GB total',search:'Search sessions or projects',titleColumn:'Session / project',aux:'Activity',metric:'Memory',group:'project'},
 tabs:{title:'Google Chrome',type:'Browser',subject:'tab',plural:'tabs',sub:'8.4 GB total',search:'Search tabs, sites, or titles',titleColumn:'Tab / site',aux:'Profile',metric:'Last viewed',group:'site'},
 ports:{title:'Listening ports',type:'Local servers',subject:'server',plural:'servers',sub:'Processes listening on your Mac',search:'Search servers, ports, or owners',titleColumn:'Server / process',aux:'Address',metric:'Port',group:'project'},
 trends:{title:'Memory trends',type:'Overview',subject:'app',plural:'apps',sub:'Changes over the last 2 hours',search:'Search apps or process groups',titleColumn:'App / process group',aux:'Change',metric:'Now',group:'kind'}
};
const direction = document.body.dataset.direction || '';
const params = new URLSearchParams(location.search);
let screen = config[params.get('screen')] ? params.get('screen') : 'sessions';
let query='',filter='all',sort='default',profile='all',inspected=null;
let selected=new Set(),expanded=new Set(),gone=new Set(),reviewTargets=[];
function read(key,fallback){try{return JSON.parse(localStorage.getItem(key))??fallback;}catch{return fallback;}}
function save(key,value){try{localStorage.setItem(key,JSON.stringify(value));return true;}catch{return false;}}
let theme=params.get('theme') || read('autotrim-table-theme',matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light');
if(!['light','dark'].includes(theme))theme='light';
let decisions=read('autotrim-table-decisions',{});
function applyTheme(){document.documentElement.dataset.theme=theme;const b=document.getElementById('theme-toggle');b.innerHTML=icon('theme')+' '+(theme==='dark'?'Light appearance':'Dark appearance');document.querySelectorAll('[data-direction-link]').forEach(a=>{const u=new URL(a.href);u.searchParams.set('screen',screen);u.searchParams.set('theme',theme);a.href=u.href;});}
function notify(message){const t=document.getElementById('toast');t.textContent=message;t.hidden=false;clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>t.hidden=true,3500);}
function live(key=screen){return datasets[key].filter(r=>!gone.has(r.id));}
function reason(r){return r.protected || (r.state==='active' && screen!=='ports' ? 'In use' : '');}
function eligible(r){return screen!=='trends' && !reason(r);}
function visible(){return live().filter(r=>(!query || [r.name,r.context,r.profile,r.group,r.port,r.address].join(' ').toLowerCase().includes(query.toLowerCase())) && (filter!=='stale'||r.state==='stale'&&eligible(r)) && (filter!=='active'||r.state==='active') && (profile==='all'||r.profile===profile)).sort((a,b)=>sort==='name'?a.name.localeCompare(b.name):sort==='idle'?b.idle-a.idle:sort==='memory'?b.memory-a.memory:screen==='tabs'?b.idle-a.idle:screen==='ports'?a.port-b.port:b.memory-a.memory);}
function checkedRows(){return visible().filter(r=>selected.has(r.id)&&eligible(r));}
function stateMarkup(r){const label=r.protected||({stale:'Stale',active:'Active',idle:'Idle'}[r.state]||'');return `<span class="row-state ${r.protected?'protected':r.state}"><i></i>${E(label)}</span>`;}
function signed(n){return (n<0?'−':'+')+memory(Math.abs(n));}
function metric(r){return screen==='tabs'?(r.state==='active'?'Now':elapsed(r.idle)+' ago'):screen==='ports'?String(r.port):memory(r.memory);}
function auxiliary(r){return screen==='tabs'?E(r.profile):screen==='ports'?E(r.address):screen==='trends'?`<span style="color:${r.change>0?'var(--amber)':'var(--muted)'}">${signed(r.change)}</span>`:stateMarkup(r)+`<span class="row-sub">${r.state==='active'?'Working now':elapsed(r.idle)+' ago'}</span>`;}
function checkbox(r){return eligible(r)?`<input type="checkbox" data-select="${r.id}" aria-label="Select ${E(r.name)}" ${selected.has(r.id)?'checked':''}>`:`<span class="protected-label" title="${E(reason(r))}" aria-label="${E(reason(r))}">${icon('lock')}</span>`;}
function titleButton(r){return `<button class="row-title" data-inspect="${r.id}" ${direction==='split'?`aria-pressed="${inspected===r.id}"`:`aria-expanded="${expanded.has(r.id)}" aria-controls="detail-${r.id}"`}>${E(r.name)}</button>`;}
function detailsMarkup(r){const facts=screen==='sessions'?[['Project',r.context],['Host',r.host],['Open',r.age],['CPU',r.cpu],['PID',r.pid],['Last activity',r.state==='active'?'Working now':elapsed(r.idle)+' ago']]:screen==='tabs'?[['Profile',r.profile],['Site',r.context],['Last viewed',r.state==='active'?'Now':elapsed(r.idle)+' ago'],['Protection',reason(r)||'None'],['≈ Memory',memory(r.memory)+' estimated']]:screen==='ports'?[['Owner',r.host],['Address',r.address],['Protocol',r.protocol],['Port',r.port],['Open',r.age],['Memory',memory(r.memory)]]:[['Now',memory(r.memory)],['Change',signed(r.change)],['Rate / hour',signed(r.rate)],['Mean CPU',r.cpu],['Window',r.span]];return {facts,note:screen==='tabs'?r.url:screen==='sessions'?'Last activity comes from the saved session transcript.':screen==='ports'?'Managed services stay under their app’s control.':'Changes compare the start and end of the observation window.'};}
function fullRow(r){const cols=screen==='trends'?4:5;const d=detailsMarkup(r);return `<tr class="${selected.has(r.id)?'selected':''}">${screen==='trends'?'':`<td class="check">${checkbox(r)}</td>`}<td class="title-cell">${titleButton(r)}<span class="row-sub">${E(r.context)}</span></td><td>${auxiliary(r)}</td><td class="num metric">${metric(r)}${screen==='sessions'?`<div class="memory-line" aria-hidden="true"><i style="width:${Math.min(100,r.memory/1300*100)}%"></i></div>`:''}</td>${screen==='trends'?`<td class="num">${signed(r.rate)}</td>`:direction==='grouped'?'':`<td class="cell-action">${eligible(r)?`<button class="text-action" data-review="${r.id}" aria-label="Review ${E(r.name)}">Review</button>`:`<span class="protected-label">${E(reason(r))}</span>`}</td>`}</tr>${expanded.has(r.id)?`<tr id="detail-${r.id}" class="detail-row"><td colspan="${direction==='grouped'&&screen!=='trends'?cols-1:cols}"><div class="detail-content">${d.facts.map(([k,v])=>`<div><small>${E(k)}</small>${E(v)}</div>`).join('')}<p>${E(d.note)}</p></div></td></tr>`:''}`;}
function headers(grouped=false){const c=config[screen];return `<thead><tr>${screen==='trends'?'':`<th class="check">${grouped?'':`<input type="checkbox" id="select-all" aria-label="Select all eligible rows">`}</th>`}<th>${grouped?(screen==='sessions'?'Session':screen==='tabs'?'Tab':c.titleColumn):c.titleColumn}</th><th>${c.aux}</th><th class="num">${c.metric}</th>${screen==='trends'?'<th class="num">Per hour</th>':grouped?'':'<th class="num">Action</th>'}</tr></thead>`;}
function quietTable(rows){return `<div class="table-scroll quiet-frame"><table>${headers()}<tbody>${rows.map(fullRow).join('')}</tbody></table></div>`;}
function groupedTable(rows){const groups=new Map();rows.forEach(r=>{if(!groups.has(r.group))groups.set(r.group,[]);groups.get(r.group).push(r);});return [...groups].map(([name,list])=>`<details class="group-section" data-group="${E(name)}" open><summary>${icon('chevron','group-chevron')}${icon(screen==='tabs'?'globe':'folder','group-title-icon')}<span class="group-title">${E(name)}</span><span class="group-summary"><span>${list.length} ${list.length===1?config[screen].subject:config[screen].plural}</span><span class="group-memory">${screen==='tabs'?'≈ ':''}${memory(list.reduce((n,r)=>n+r.memory,0))}${screen==='tabs'?' estimated':''}</span></span></summary><div class="table-scroll"><table>${headers(true)}<tbody>${list.map(fullRow).join('')}</tbody></table></div></details>`).join('');}
function inspector(r){if(!r)return '<aside class="inspector inspector-empty">Select a name to see its details.</aside>';const d=detailsMarkup(r);return `<aside class="inspector" aria-label="Selected ${config[screen].subject} details"><div class="inspector-kind"><span>${config[screen].type}</span>${screen==='trends'?icon('history'):stateMarkup(r)}</div><h3>${E(r.name)}</h3><p class="context">${E(r.context)}</p><div class="inspector-memory">${screen==='tabs'?'≈ ':''}${memory(r.memory)}<small>${screen==='tabs'?'Estimated share of browser memory':'In memory now'}</small></div><dl>${d.facts.filter(([key])=>!['Project','≈ Memory','Memory','Now'].includes(key)).map(([k,v])=>`<div><dt>${E(k)}</dt><dd>${E(v)}</dd></div>`).join('')}</dl><div class="recovery">${icon('history')}${E(screen==='sessions'?'The transcript stays on disk. Resume instructions are saved when a session closes.':screen==='tabs'?'Reopen with ⌘⇧T in the same profile. The URL is saved in Actions.':screen==='ports'?'Review the process before stopping it. Managed services stay open.':d.note)}</div>${screen!=='trends'?`<button ${eligible(r)?'class="primary"':'disabled'} data-review="${r.id}">${eligible(r)?'Review '+(screen==='ports'?'stop':'close'):E(reason(r))+' · kept open'}</button>`:''}</aside>`;}
function splitTable(rows){if(!rows.some(r=>r.id===inspected))inspected=rows[0]?.id;return `<div class="split-layout"><div class="table-scroll quiet-frame"><table class="compact-table"><thead><tr>${screen==='trends'?'':'<th class="check"><input type="checkbox" id="select-all" aria-label="Select all eligible rows"></th>'}<th>${config[screen].titleColumn}</th><th class="num">${config[screen].metric}</th></tr></thead><tbody>${rows.map(r=>`<tr class="${r.id===inspected?'inspected':''} ${selected.has(r.id)?'selected':''}">${screen==='trends'?'':`<td class="check">${checkbox(r)}</td>`}<td class="title-cell">${titleButton(r)}<span class="row-sub"><i class="state-symbol ${r.state||''}" aria-hidden="true"></i>${r.state==='stale'?`<span class="stale-time" title="${screen==='tabs'?'Last viewed':'Last activity'} ${E(elapsed(r.idle))} ago" aria-label="Stale · ${screen==='tabs'?'last viewed':'last activity'} ${E(elapsed(r.idle))} ago">${E(compactElapsed(r.idle))}</span>`:''}<span class="row-context">${E(r.context)}</span></span></td><td class="num">${metric(r)}</td></tr>`).join('')}</tbody></table></div>${inspector(rows.find(r=>r.id===inspected))}</div>`;}
function navMarkup(){return `<nav class="app-nav" aria-label="App navigation"><button class="nav-item ${screen==='trends'?'selected':''}" data-screen="trends">${icon('overview')}Overview</button><button class="nav-item ${screen==='ports'?'selected':''}" data-screen="ports">${icon('ports')}Listening ports</button><a class="nav-item" href="../03-actions.html#history">${icon('history')}Actions</a><a class="nav-item" href="../03-actions.html#settings">${icon('settings')}Settings</a><div class="nav-heading"><span>Largest holders</span><span>Memory ↓</span></div><button class="nav-item holder browser ${screen==='tabs'?'selected':''}" data-screen="tabs"><i class="dot"></i><span class="name">Google Chrome</span><span class="num">8.4 GB</span><small>${live('tabs').length} tabs, 2 profiles</small></button><button class="nav-item holder ${screen==='sessions'?'selected':''}" data-screen="sessions"><i class="dot"></i><span class="name">Claude Code</span><span class="num">6.2 GB</span><small>${live('sessions').length} sessions</small></button>${[['Codex','3.1 GB','2 sessions'],['Cursor','2.0 GB','24 processes'],['Docker','1.3 GB','8 processes']].map(([name,mem,detail])=>`<div class="nav-item holder"><i class="dot"></i><span class="name">${name}</span><span class="num">${mem}</span><small>${detail}</small></div>`).join('')}<div class="nav-foot"><i></i>Running in background<br>Auto mode off</div></nav>`;}
function headerMarkup(){return `<div class="memory-header"><div class="physical"><div class="memory-label"><span><b>24.8 GB</b> of 32 GB</span><span>In use</span></div><div class="memory-track"><i></i><i></i></div><div class="memory-sub"><span>Includes 4.1 GB compressed</span><span>7.2 GB unused</span></div></div><div class="memory-stat"><b>1.2 GB</b><small>Swap on disk</small></div><div class="memory-stat cpu"><b>14%</b><small>CPU</small></div><div class="freshness">Updated just now</div></div>`;}
function mainMarkup(){const c=config[screen];return `<div class="page-heading"><h2>${c.title}<span class="type-label">${c.type}</span></h2></div><p class="page-sub">${c.sub} <span style="margin-left:13px">${live().length} ${c.plural}</span></p><div class="list-tools"><label class="search">${icon('search')}<input id="search" type="search" aria-label="${c.search}" placeholder="${c.search}" value="${E(query)}"></label>${screen==='tabs'?`<select id="profile" aria-label="Filter by profile"><option value="all">All profiles</option><option value="Work" ${profile==='Work'?'selected':''}>Work</option><option value="Personal" ${profile==='Personal'?'selected':''}>Personal</option></select>`:''}<select id="sort" aria-label="Sort rows"><option value="default">${screen==='tabs'?'Longest untouched':screen==='ports'?'Port number':'Most memory'}</option><option value="name" ${sort==='name'?'selected':''}>Name</option>${screen==='sessions'?`<option value="idle" ${sort==='idle'?'selected':''}>Longest idle</option>`:''}</select></div><div id="table-meta" class="table-meta"></div><div id="rows-area"></div><div id="list-footer" class="list-footer"></div>`;}
function renderApp(){document.getElementById('app-window').innerHTML=headerMarkup()+`<div class="app-body">${navMarkup()}<main class="preview-main" id="preview-main"></main></div>`;document.getElementById('preview-main').innerHTML=mainMarkup();document.querySelectorAll('[data-screen]').forEach(b=>{b.setAttribute('aria-pressed',String(b.dataset.screen===screen));b.onclick=()=>changeScreen(b.dataset.screen);});document.getElementById('search').oninput=e=>{query=e.target.value;renderRows();};document.getElementById('sort').onchange=e=>{sort=e.target.value;renderRows();};const p=document.getElementById('profile');if(p)p.onchange=e=>{profile=e.target.value;renderRows();};renderRows();applyTheme();}
function renderRows(){const rows=visible(),picked=checkedRows();document.getElementById('table-meta').innerHTML=picked.length?`<div class="selection-bar"><span>${picked.length} selected ${screen==='sessions'?`<span class="muted" style="margin-left:8px">${memory(picked.reduce((n,r)=>n+r.memory,0))} held</span>`:''}</span><div><button class="quiet" id="clear-selection">Clear</button><button class="primary" id="review-selection">Review selected</button></div></div>`:`<div class="segmented" aria-label="Filter rows">${(screen==='trends'?[['all','All apps']]:[['all','All '+config[screen].plural],['stale',screen==='ports'?'Old, unmanaged':'Stale, eligible'],['active','Active']]).map(([value,label])=>`<button data-filter="${value}" aria-pressed="${filter===value}">${label}</button>`).join('')}</div><span class="muted">${rows.length} shown</span>`;
 document.getElementById('rows-area').innerHTML=rows.length?(direction==='grouped'?groupedTable(rows):direction==='split'?splitTable(rows):quietTable(rows)):'<div class="empty">No matches. Try another search or filter.</div>';
 document.getElementById('list-footer').innerHTML=`<span>${screen==='trends'?'Measured memory across the same observation window.':screen==='tabs'?'Pinned and active tabs stay open. Memory estimates appear in details.':screen==='ports'?'App-managed and system services stay open.':'Select a name for details. Active sessions and app engines stay open.'}</span>${screen!=='trends'&&rows.some(eligible)?'<button id="select-visible" class="text-action">Select eligible rows</button>':''}`;
 document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;renderRows();document.querySelector(`[data-filter="${filter}"]`)?.focus({preventScroll:true});});document.querySelectorAll('[data-select]').forEach(cb=>cb.onchange=()=>{cb.checked?selected.add(cb.dataset.select):selected.delete(cb.dataset.select);renderRows();document.querySelector(`[data-select="${cb.dataset.select}"]`)?.focus({preventScroll:true});});const all=document.getElementById('select-all');if(all){const candidates=rows.filter(eligible);all.checked=candidates.length>0&&candidates.every(r=>selected.has(r.id));all.indeterminate=picked.length>0&&picked.length<candidates.length;all.disabled=!candidates.length;all.onchange=()=>{candidates.forEach(r=>all.checked?selected.add(r.id):selected.delete(r.id));renderRows();};}
 document.querySelectorAll('[data-inspect]').forEach(b=>b.onclick=()=>{if(direction==='split')inspected=b.dataset.inspect;else expanded.has(b.dataset.inspect)?expanded.delete(b.dataset.inspect):expanded.add(b.dataset.inspect);renderRows();document.querySelector(`[data-inspect="${b.dataset.inspect}"]`)?.focus({preventScroll:true});});document.querySelectorAll('[data-review]').forEach(b=>b.onclick=()=>openReview(visible().filter(r=>r.id===b.dataset.review&&eligible(r))));const c=document.getElementById('clear-selection');if(c)c.onclick=()=>{selected.clear();renderRows();};const v=document.getElementById('select-visible');if(v)v.onclick=()=>{rows.filter(eligible).forEach(r=>selected.add(r.id));renderRows();};const rv=document.getElementById('review-selection');if(rv)rv.onclick=()=>openReview(checkedRows());}
function changeScreen(value){screen=value;query='';filter='all';sort='default';profile='all';selected.clear();expanded.clear();inspected=null;renderApp();}
function openReview(rows){if(!rows.length)return;reviewTargets=rows.map(r=>r.id);const d=document.getElementById('sample-review');d.innerHTML=`<h2>Review ${rows.length} ${rows.length===1?config[screen].subject:config[screen].plural}</h2><p>This is a design preview using sample data.</p><ul>${rows.map(r=>`<li>${E(r.name)}<small>${E(r.context)}${r.profile?' · '+E(r.profile):''}</small></li>`).join('')}</ul><p class="dialog-note">${screen==='sessions'?'Transcripts stay on disk, with recovery instructions in Actions.':screen==='tabs'?'Use ⌘⇧T in the same profile to reopen a tab. Its URL is saved in Actions.':'The process and its owner are checked again before stopping.'}</p><div class="dialog-buttons"><button id="cancel-review" autofocus>Cancel</button><button class="primary" id="simulate-close">Simulate ${screen==='ports'?'stop':'close'}</button></div>`;d.showModal();document.getElementById('cancel-review').onclick=()=>d.close();document.getElementById('simulate-close').onclick=()=>{reviewTargets.forEach(id=>{gone.add(id);selected.delete(id);});d.close();renderApp();notify('Sample rows removed. Reset sample restores them.');};}
function renderDecision(){const entry=decisions[direction]||{};document.querySelectorAll('[data-decision]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.decision===entry.decision)));const status=document.getElementById('decision-status');if(status)status.textContent=entry.decision?'Saved for this design: '+entry.decision+'. Export your review to share it back here.':'Choose a direction or leave notes. Decisions save in this browser; they do not change autoTrim.';}
function exportReview(){const lines=['# autoTrim table redesign review','',...directions.flatMap(d=>['## '+d.title,'Decision: '+(decisions[d.id]?.decision||'Not reviewed'),'Notes: '+(decisions[d.id]?.notes||'—'),'']),'Mockups only. No application changes requested by this file.'];const u=URL.createObjectURL(new Blob([lines.join('\n')],{type:'text/markdown'}));const a=document.createElement('a');a.href=u;a.download='autotrim-table-review.md';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000);}
document.getElementById('theme-toggle').onclick=()=>{theme=theme==='dark'?'light':'dark';save('autotrim-table-theme',theme);applyTheme();};document.querySelectorAll('[data-export]').forEach(b=>b.onclick=exportReview);applyTheme();
if(direction){renderApp();document.getElementById('reset-sample').onclick=()=>{gone.clear();selected.clear();expanded.clear();inspected=null;query='';filter='all';profile='all';renderApp();notify('Sample restored.');};document.querySelectorAll('[data-decision]').forEach(b=>b.onclick=()=>{decisions[direction]={...decisions[direction],decision:b.dataset.decision};save('autotrim-table-decisions',decisions);renderDecision();});const notes=document.getElementById('feedback');notes.value=decisions[direction]?.notes||'';notes.oninput=()=>{decisions[direction]={...decisions[direction],notes:notes.value};save('autotrim-table-decisions',decisions);};renderDecision();}
